
# Watch all

Create a companion agent in the same folder as you are
Type = shell
Command = make watch-all

This command will run various quality gates like linters and tests in watch mode, so that they re-run automatically when you change files.

So be creative when naming this agent.
